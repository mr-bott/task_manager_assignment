# TaskFlow - Mini SaaS Task Management System

A secure, scalable, and elegantly designed full-stack web application where users can sign up, log in, and manage their tasks.

## 🚀 Tech Stack

### Frontend
- **React.js** (Vite)
- **Tailwind CSS** for modern SaaS UI (Glassmorphism, soft shadows)
- **React Router DOM** for routing
- **Redux Toolkit** for global state management
- **Axios** for API requests with interceptors
- **React Hook Form + Yup** for form validation
- **React Hot Toast** for notifications
- **Lucide React** for beautiful icons
- **Date-fns** for date formatting

### Backend
- **Node.js & Express.js**
- **PostgreSQL** with **Sequelize ORM**
- **JWT Authentication** (Secure HTTP headers)
- **Bcrypt.js** for password hashing
- **Winston & Morgan** for centralized logging (console + files)
- **Joi** for backend validation
- **Helmet** & **CORS** for security
- **Express Rate Limit** to prevent abuse

## 📂 Project Structure

```
task_manager/
├── backend/
│   ├── logs/               # Auto-generated by Winston
│   ├── src/
│   │   ├── config/         # App & DB Config
│   │   ├── controllers/    # Route logic
│   │   ├── middlewares/    # Auth & Error Handlers
│   │   ├── models/         # Sequelize Models
│   │   ├── routes/         # Express Routes
│   │   ├── services/       # Business logic (optional)
│   │   ├── utils/          # Helpers (Logger, API Response, etc.)
│   │   ├── validators/     # Joi Schemas
│   │   ├── app.js          # Express App setup
│   │   └── server.js       # Entry point
│   ├── .env                # Environment variables
│   └── package.json
└── frontend/
    ├── src/
    │   ├── components/     # Reusable UI components
    │   ├── pages/          # React routes (Login, Register, Dashboard)
    │   ├── store/          # Redux Store & Slices
    │   ├── utils/          # Axios config & Interceptors
    │   ├── App.jsx         # Main App component
    │   └── main.jsx        # Entry point
    ├── tailwind.config.js
    └── package.json
```

## 🛠️ Setup Guide

### Prerequisites
- Node.js (v18+ recommended)
- PostgreSQL (Ensure it is running)

### 1. Database Setup
Create a PostgreSQL database named `taskmanager_db`:
```sql
CREATE DATABASE taskmanager_db;
```

### 2. Backend Setup
1. Navigate to the backend directory:
   ```bash
   cd backend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Check the `.env` file (already created with defaults) and update `DB_PASSWORD` if necessary:
   ```env
   PORT=5000
   NODE_ENV=development
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=taskmanager_db
   DB_USER=postgres
   DB_PASSWORD=yourpassword
   JWT_SECRET=your_super_secret_jwt_key_here_min_32_chars_long
   JWT_EXPIRES_IN=7d
   CORS_ORIGIN=http://localhost:5173
   ```
4. Start the backend server:
   ```bash
   npm run dev
   ```
   *The server will start on port 5000 and automatically sync the Sequelize models.*

### 3. Frontend Setup

 Check the `.env` file
   ```env
   VITE_API_URL=http://localhost:5000/api
   ```
   
1. Open a new terminal and navigate to the frontend directory:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the frontend development server:
   ```bash
   npm run dev
   ```
   *The frontend will start on port 5173.*

## 🌟 Key Features Implemented

- **Authentication Flow:** Complete JWT-based signup, login, and secure logout.
- **Global Error Handling:** Consistent API error responses and frontend toast notifications.
- **Data Validation:** Strict validation using Joi (Backend) and Yup (Frontend).
- **Beautiful UI:** Responsive modern SaaS design, glassmorphism cards, animated transitions, loading states.
- **State Management:** Fully integrated Redux Toolkit for auth state and task management.
- **Centralized Logging:** Winston logging to files (`combined.log`, `error.log`) and console.
- **Security Best Practices:** Password hashing, rate limiting, helmet headers, CORS configuration.
